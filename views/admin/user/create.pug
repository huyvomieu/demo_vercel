extends ../layouts/main.pug
block content
    .form.my-3
        form(class= "form" action=`/admin/user/create`, method="post")
            .form-group
                label() Email
                input.form-control(type="text", placeholder="", id="email", name="email" )
            .form-group
                label() FullName
                input.form-control(type="text", placeholder="", id="fullname", name="fullname" )
            .form-group
                label() Ngày  tạo
                input.form-control(type="text", placeholder="", id="createdAt", name="createdAt" )
           
            button(type="submit" class="btn btn-primary") Create
